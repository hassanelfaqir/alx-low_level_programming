#include <stdio.h>

/**
 * main - Entry point
 *
 * descreption - digit numbers of base 10 starting from 0
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int in = 0;

	while (in < 10)
	{
		printf("%i", in);
		in++;
	}
	printf("\n")

	return (0);
}
